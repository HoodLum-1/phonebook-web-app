spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    spring:
      datasource:
        username: sa
        password:
